<p id="notice"><%= notice %></p>

<h1>Owners</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @owners.each do |owner| %>
      <tr>
        <td><%= owner.id %></td>
        <td><%= owner.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to "Catページへ", cats_url %>
<%= link_to "Dogページへ", dogs_path %>
